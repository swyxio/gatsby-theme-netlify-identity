{
  "private": true,
  "workspaces": [
    "gatsby-theme-netlify-identity",
    "gatsby-theme-tyra",
    "demo"
  ],
  "scripts": {
    "start": "yarn workspace demo start",
    "build": "yarn workspace demo build",
    "version": "echo 'go into gatsby-theme-netlify-identity and run npm version'"
  }
}
